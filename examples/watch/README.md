# Watch mode example

This example demonstrates how to use gulp's watch mode using plugin `gulp-esbuild`.

## How to use

1. `npm i` - installs dependencies
2. `npm run build` - run watch mode
3. change some file in `src` folder
