{
  "private": true,
  "scripts": {
    "build-default-metafile": "gulp build-default-meta",
    "build-named-metafile": "gulp build-named-meta"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "gulp": "^4.0.2",
    "gulp-esbuild": "latest"
  }
}
